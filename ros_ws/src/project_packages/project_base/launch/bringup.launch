<launch>
    <!-- SIMULATION -->
    <!-- ARGUMENTS -->
    <arg name="simulation" default="true" />
    <arg name="stack" default="true" />

    <!-- NODES and LAUNCH FILES --> 
    <include file="$(find project_base)/launch/simulation_base.launch" if="$(arg simulation)"> </include>

    <!-- Project stack -->
    <include if="$(arg stack)" file="$(find project_base)/launch/stack.launch"> </include>
    
    <!-- OTHERS -->
    <!-- ARGUMENTS -->
    <arg name="rviz" default="true"/>
    <arg name="image_view" default="false"/>

    <!-- Start RViz  -->
    <node if="$(arg rviz)" name="rviz" pkg="rviz" type="rviz" output="screen" args="-d $(find project_base)/rviz/video_servoing.rviz"/>
    <!-- start image_view node -->
    <node if="$(arg image_view)" name="image_view" pkg="image_view" type="image_view" output="screen" args="image:=/camera/image_raw"/>
</launch>
